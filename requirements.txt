# Web 框架
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# 数据校验与配置
pydantic>=2.0
pydantic-settings>=2.0

# 推理引擎（Jetson 嵌入式环境）
numpy>=1.20.0
transformers>=4.30.0   # tokenizer
# tensorrt  # Jetson 上随 JetPack 预装，x86 可 pip install tensorrt
# pycuda    # pip install pycuda
